<Window x:Class="EventDrivenProgEnrollmentSystem.Transaction"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EventDrivenProgEnrollmentSystem"
        mc:Ignorable="d"
        Title="Transaction" Height="550" Width="800" Background="#F6F6FA"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- style for those little white cards -->
        <Style x:Key="PaymentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4A5BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="6"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <!--Header-->
        <Border Grid.Row="0" Background="#C7B9FF">
            <StackPanel Orientation="Horizontal"  VerticalAlignment="Center" Margin="2,0">
                <Image Source="C:\Users\Robin\Downloads\unisouthlogo.png" Height="80" Width="80" Stretch="Uniform"/>
                <TextBlock Text="UniSouth" FontSize="25" FontWeight="Bold" Foreground="#2D2D2D" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,20,0"></TextBlock>
            </StackPanel>
        </Border>


        <StackPanel Grid.Row="1" Margin="40,20,40,10">
            <StackPanel>
                <TextBlock Text="Tuition Fee - Send to: UniSouth" FontSize="28" Margin="0,0,0,8"/>
                <Rectangle Height="1" Fill="#888" HorizontalAlignment="Stretch" Margin="0,0,0,16"/>
            </StackPanel>

            

            <TextBlock Text="Email Address" FontSize="14" Margin="0,15,0,4"/>
            <TextBox x:Name="txtEmail" Width="400" Height="30" Padding="4" BorderBrush="{x:Null}" BorderThickness="1" Margin="0,0,0,16" HorizontalAlignment="Left"/>

            <ComboBox x:Name="PaymentOptionsComboBox" Width="400" Height="40" Padding="8,0" Background="Transparent" HorizontalAlignment="Left" >
                <ComboBoxItem Content="Plan A - 80000 Pesos, 0 Balance"/>
                <ComboBoxItem Content="Plan B - 50000 Pesos, 30000 Balance"/>
                <ComboBoxItem Content="Plan C - 30000 Pesos Initial, 10,0000 Pesos Montly"/>
            </ComboBox>

            <Button Content="Send For Confirmation" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,80,0,50" Width="140" Height="40" Style="{StaticResource PrimaryButton}" Cursor="Hand" Click="Confirmation_Click"></Button>
            
            <!--<TextBlock Text="Payment Options" FontSize="14" Margin="0,0,0,4"/>
            <ComboBox x:Name="PaymentOptionsComboBox" Width="400"
              Height="40"
              Padding="8,0"
              Background="Transparent"
              HorizontalAlignment="Left" SelectionChanged="PaymentOptionsComboBox_SelectionChanged">
                <ComboBoxItem Content="Card"/>
                <ComboBoxItem Content="Mobile"/>
            </ComboBox>-->
           



            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,30,0,0">
                <TextBlock Text="Pay with" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button Style="{StaticResource PaymentButtonStyle}" Margin="0,0,10,0" Click="Visa_Click">
                    <Image Source="C:\Users\Robin\Downloads\visa.png" Height="36" Width="60" Stretch="Uniform"/>
                </Button>
                <Button Style="{StaticResource PaymentButtonStyle}" Margin="0,0,10,0">
                    <Image Source="C:\Users\Robin\Downloads\paypal.png" Height="36" Width="60" Stretch="Uniform"/>
                </Button>
                <Button Style="{StaticResource PaymentButtonStyle}" Click="Amex_Click">
                    <Image Source="C:\Users\Robin\Downloads\amex.png" Height="36" Width="60" Stretch="Fill"/>
                </Button>
            </StackPanel>

            
        
        
       
        <!--<Border Grid.Row="1" Margin="40,16">
            <StackPanel Margin="24">
                <TextBlock Text="Tuition Fee" FontSize="30"/>
                <Rectangle Height="2" Fill="#888" HorizontalAlignment="Stretch" />

                <TextBlock Text="Email"/>
                <TextBox Height="30" Width="300" HorizontalAlignment="Left"/>

                <TextBlock Text="Payment Options"/>
                <ComboBox  
                  VerticalAlignment="Top" 
                  HorizontalAlignment="Left"        
                  Width="200">
                    <ComboBoxItem Content="Card"/>
                    <ComboBoxItem Content="Mobile"/>
                    <ComboBoxItem Content="Option 3"/>
                </ComboBox>
            </StackPanel>
        </Border>-->
            


        </StackPanel>
        
        
    </Grid>
</Window>
