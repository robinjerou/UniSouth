<Window x:Class="EventDrivenProgEnrollmentSystem.DocumentUpload"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EventDrivenProgEnrollmentSystem"
        mc:Ignorable="d"
        Title="DocumentUpload" Height="450" Width="800" Background="#F6F6FA">

    <Window.Resources>
        <!-- Card style (lavender) -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="#E1D2FF"/>
            <Setter Property="CornerRadius" Value="12"/>
        </Style>
        <!-- Primary “Send Files” button style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4A5BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- optional hover effect -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1">
            <Button Content="Submit Files" VerticalAlignment="Center" Margin="550,5,0,310" Width="140" Height="40" Style="{StaticResource PrimaryButton}" Click="SubmitButton_Click" Cursor="Hand"></Button>
        </StackPanel>



        <!--Header-->
        <Border Grid.Row="0" Background="#C7B9FF">
            <StackPanel Orientation="Horizontal"  VerticalAlignment="Center" Margin="2,0">
                <Image Source="C:\Users\Robin\Downloads\unisouthlogo.png" Height="80" Width="80" Stretch="Uniform"/>
                <TextBlock Text="UniSouth" FontSize="25" FontWeight="Bold" Foreground="#2D2D2D" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,20,0"></TextBlock>
            </StackPanel>
        </Border>

        <!--Title and Content Container-->
        <Border Grid.Row="1"  Background="#E1D2FF" CornerRadius="12" Margin="50">
            <StackPanel Margin="24" VerticalAlignment="Top">
                <TextBlock Text="Send Required Documents" FontSize="20" FontWeight="SemiBold" TextAlignment="Center"/>
                <Rectangle Height="1" Width="320" Fill="#888" HorizontalAlignment="Center" Margin="0,4,0,12"/>

                <Border Background="White" BorderBrush="#AAA" Height="180" CornerRadius="10" BorderThickness="2">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Upload Files" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" Height="70"/>
                        <TextBlock Text="Choose a file to upload" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="PDF, WORD, and etc." FontSize="14" HorizontalAlignment="Center" Height="40"/>

                        <Button Content="Browse Files" Height="25" Style="{StaticResource SecondaryButton}" Click="FileUploadButton_Click"/>

                        <ListView x:Name="lvUploads" Margin="0,12,0,0" Background="White" BorderThickness="0">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="4" Padding="10" Background="#F3F3FF" CornerRadius="8">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Image Source="/Assets/pdf-icon.png" Height="32" Width="32" Margin="0,0,10,0"/>
                                            <StackPanel Width="500">
                                                <TextBlock Text="{Binding FileName}" FontWeight="SemiBold"/>
                                                <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress}" Height="8" Margin="0,4,0,0"/>
                                                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="Gray"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
        
    </Grid>
</Window>
